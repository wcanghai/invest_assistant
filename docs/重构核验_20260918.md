# 2026-09-18 继续修复与核验记录

## 已完成的工程修复

- 主业务代码已实际迁入 `src/invest`，不是旧目录转发壳；数据访问和 SQL 资源移入 storage。
- 根目录旧源码已归档至 `backups/module_relocation_20260917/legacy_source/`。
  切换前源码压缩备份为同级 `source_before.zip`，保留当时两个任务的 XML。
- 正式每日行情和日报补跑不再反向调用 PowerShell；共用行情锁和完整性检查，
  部分失败保持可重试状态。Windows 短暂占用状态文件时有限重试原子替换。
- 参数、策略名与输入格式保留，补齐交易日辅助、财务增量、行情状态及报告校验入口。
- 研究输出新增独立运行档案；修正旧库默认路径、固定研究日期输出目录以及外部启动目录问题。
- 依赖按照采集、研究、日报、开发分组，并用本机已验证版本生成 `requirements-lock.txt`。

## 已取得的验证证据

1. 归档旧源码后，90 项测试通过，包含原业务测试、迁移、分库、调度、研究档案、
   Windows 文件占用重试、市场只读及显式相对路径测试。
2. 17 组旧/新实现独立比较通过：股票因子、四种 MVP 策略及组合、股票回测净值与成交、
   ETF 证券池、八种 ETF 策略的信号和回测、三因子 ETF 轮动。
   容差为 `rtol=1e-10, atol=1e-12`；离散值和缺失位置一致。
   只忽略股票回测的随机运行/订单编号。
3. 使用 SQLite 备份接口制作正式市场库副本，在副本上实际完成
   `000001.SZ` 的 2026-09-17 日线/股本/交易数据更新，以及 `510300.SH` 的限定日期补采。
   未使用正式结果库或账户库进行这些试验。
4. Python 编译、94 个 Python 文件的编码规范检查通过；依赖检查没有冲突。
   Wheel 构建成功，包含四个 SQL 文件、日报静态资源和全部新包模块。
5. 原端口 8766 新网站返回 HTTP 200；19 份已有报告可读，原 18 份历史版本仍在。
6. 两个正式定时任务于 2026-09-18 07:18 恢复，任务名称、触发器和权限未重建。
   07:20 行情守护任务按计划自行触发，启动器退出码为 0。
   后续 07:38 专项检查发现，该退出码曾掩盖后台参数错误，因此不能作为业务成功证据。
   已修复后台参数构造并让 VBS 传递失败退出码；重新触发后取得真实的 `not_due`
   业务日志和空错误日志，确认早间正确跳过当日盘后更新。
   日报任务已发起 9 月 17 日的行情补跑，业务状态记录位于
   `data/state/market/2026-09-17.json`，完成与否以该状态和实际完整性检查为准。

## 数据迁移结论与界限

此前四库正式迁移已核对 37 张业务表、49,208,948 行；逐表内容、主键顺序摘要、
结构、索引、触发器、自增序列核对通过。固定备份和成功清单仍保留。
本轮没有重新迁移或覆盖四个正式库；恢复后的正常采集会产生预期增量。
不能拿固定迁移基线与持续更新中的正式库比较后，把新增行误判为迁移失败。

SQLite 已执行 `quick_check` 和外键检查，尚未把长时间的完整 `integrity_check`
作为完成证据。不能声称已完成这一项。账户库未发现需要自动导入的额外正式账户源。

## 尚未宣告完成的项目

- 9 月 17 日全市场补采及后续日报的最终业务结果，需在运行结束后核验；
  通达信连接成功和任务退出码不能替代全市场数据完整性。
- 手工全历史采集脚本仍有 PowerShell 编排，旧的一次性研究生成脚本仍需分类整理。
- 全套真实历史数据上的全部策略重新计算，尚未替代为已完成；本次一致性证据来自固定合成输入。
- 完整数据库 `integrity_check`、独立干净环境重装、全量生产回滚演练仍待后续维护窗口验证。

以上区分已验证修复与剩余验收项，本记录不宣称整份长期重构方案已全部验收。
